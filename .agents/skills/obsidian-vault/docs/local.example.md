# obsidian-vault — Local Configuration Reference
#
# Copy this file to references/local.md and customize for this machine.
# references/local.md is gitignored — it never leaves this machine.
#
# VAULT_PATH is required. All other keys are optional.

# ─── Vault Location ───────────────────────────────────────────────────────────

# Absolute path to your Obsidian vault root directory (required).
VAULT_PATH=/Users/yourname/Documents/YourVault

# ─── Default Note Paths ──────────────────────────────────────────────────────

# Default destination for email digests (used by read-apple-mail when ENABLE_OBSIDIAN_SYNC=true).
# Tokens: <YYYY>, <MM>, <DD>
# Default: Inbox Summaries/Email/<YYYY>-<MM>-<DD> Email Digest.md
# MAIL_DIGEST_NOTE_PATH=Inbox Summaries/Email/<YYYY>-<MM>-<DD> Email Digest.md

# Default destination for daily plan notes (used by prepare-daily-plan if integrated).
# DAILY_PLAN_NOTE_PATH=Daily Notes/<YYYY>-<MM>-<DD>.md
